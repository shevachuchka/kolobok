#include <stdio.h>

int main(void)
{
    int a;
    int b;
    int c = 0;

    printf("A: ");
    scanf("%i", &a);

    for (int i = 1; c < a; i++)
    {
        c += i;
        b = i;
    }

    printf("b =%i \n", b);
    printf("c =%i", c);
}
